#include <bits/stdc++.h>
 
using namespace std;
typedef long long ll;


int main() {
    int k;
    cin >> k;
    while(k--) {
        int n;
        cin >> n;
        
        vector<int> v(n);
        for (int &val : v) {
            cin >> val;
        }
        
        
    }

    return 0;
}
