#include <bits/stdc++.h>
 
using namespace std;
typedef long long ll;
 
int32_t main() {
    int n, m, k;
    cin >> n >> m >> k;
    if(m >= n && k >= n)
        cout << "YES";
    else 
        cout << "NO";
}
