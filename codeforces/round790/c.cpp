#include <bits/stdc++.h>
 
using namespace std;
typedef long long ll;
 
int32_t main() {
    int t;
    cin >> t;
    while(t--) {
        int n, m;
        cin >> n >> m;
        
        vector<string> v(n);
        for (string &val : v) {
            cin >> val;
        }
        
        for (int i = 0; i < v.size(); i++) {
            
        }
    }
}
