#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main() {
    int tt;
    cin >> tt;
    while(tt--) {
        int n;
        cin >> n;
        
        vector<int> v(n);
        for (int &val : v) {
            cin >> val;
        }
        
        for (int i = 0; i < n; i++) {
            
        }

    }

    return 0;
}
